const initialState = {
    objects: [{
        "width": 163,
        "height": 84,
        "rotate": 0,
        "strokeWidth": 0,
        "fill": "rgba(0, 123, 255, 1)",
        "radius": "0",
        "blendMode": "normal",
        "type": "rectangle",
        "x": 17,
        "y": 15
    }]
}

export default (state = initialState, action) => {
    switch (action.type) {
      
        default:
            return state
    }
}
